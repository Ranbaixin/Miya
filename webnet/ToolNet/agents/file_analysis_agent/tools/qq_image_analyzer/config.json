{
    "type": "function",
    "function": {
        "name": "qq_image_analyzer",
        "description": "分析QQ图片内容。需要提供图片URL(引用消息中的图片URL)。当用户发送引用消息带有图片并要求分析时，必须调用此工具。",
        "parameters": {
            "type": "object",
            "properties": {
                "image_url": {
                    "type": "string",
                    "description": "图片的URL地址(从引用消息中获取)"
                }
            },
            "required": ["image_url"]
        }
    }
}