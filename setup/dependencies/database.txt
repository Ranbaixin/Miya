# Miya - Database Dependencies
# 当前存储后端：JSON 文件 + SQLite
# 外部数据库（Redis / Milvus / Neo4j / ChromaDB）已不再使用
# Install: pip install -r setup/dependencies/database.txt

# Vector Database (已废弃，不再加载)
# chromadb  - 已移除
# pymilvus  - 已移除（向量搜索通过 SQLite + 余弦相似度实现）

# Graph Database (已废弃，不再加载)
# neo4j     - 已移除（知识图谱功能已关闭）

# Cache (已废弃，不再加载)
# redis     - 已移除（缓存使用内存 + SQLite）

# ORM / Data Access
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
sqlmodel>=0.0.16
asyncpg>=0.29.0
aiomysql>=0.2.0

# Data Analysis
pandas>=2.0.0,<3.0.0
scipy>=1.10.0,<2.0.0
scikit-learn>=1.3.0,<1.6.0
