# Miya 项目会话总结日志

> 分支: Ranxin (53 commits) | v8.0.0 | Python 3.13 + DeepSeek V4 Flash
> 会话跨度: 2026-06-10 ~ 06-27 | 备份: backup-20260627-final

---

## 一、完成的核心工作

### 项目基础
- 版本号 v7.0->v8.0 全局统一 (32文件)
- UTF-8 BOM 清除 (6文件)
- .gitignore 补漏 (防误提交 API Key)
- 用户名「佳」->「然鑫」全局替换, 保护复合词

### Neo4j 知识图谱
- 修复端口 7687->17687, 连接 Docker 容器
- MemoryNet 注入 grag_memory, 打通对话->Neo4j 管道
- LLM 五元组提取中英文 key 兼容
- 验证: 我喜欢看电影 -> 3条五元组存入 Neo4j

### 性能优化
- 统一配置缓存 (9->1次 I/O)
- working_memory 写入防抖 (每条->5秒批)
- 索引批量刷盘 (10x 减少 I/O)
- SQLite WAL 模式 + busy_timeout

### 安全加固
- shell=True -> shlex.split + shell=False
- exec() 沙箱 (安全 builtins)
- 凭据环境变量化, CORS 修复

### 记忆系统
- 记忆锚点 mtime 同步 (JSON 更新即时生效)
- historian level 持久化 (update 新增 level 参数)
- session_manager 单例修复
- 记忆用户关联 (存与检索均按 user_id)

### 身份识别 (三层)
- 创造者(869135903)/助理(3141293329)/普通用户
- 系统提示词 {owner_name} 动态注入, 不硬编码
- 群聊说话者标注 [sender_name]

### QQ/OneBot 连接
- 新增强反向 WS 监听 (8095), NapCat 主动连接
- OneBot JSON 解析容错
- message_batcher 防丢消息
- 重连指数退避 + 随机抖动
- 健康检查适配反向 WS 模式

### 前端修复
- CoreApiClient 15 缺失方法 + 端口统一 9800
- Live2D 禁用 (缺失文件/噪音)
- Desktop 终端回退到 Python

### MCP 服务
- 新增 PC Time Tracker (6工具)
- OpenClaw 路径环境变量化

### 审计
- 6轮 Workflow, 28 Agent, 455 项发现
- 43+ 项修复, 守护进程自检通过

---

## 二、开放问题

| 问题 | 状态 |
|------|:---:|
| QQ 用户消息回复可能未送达 (5条生成, 仅3条发送-均为主动聊天) | 排查中, 已在 _send_onebot_reply 加日志 |
| jieba 模块缺失 -> 表情包发送失败 | 需 pip install |
| miya_api.py 30路由被 stub 覆盖 -> Dashboard 无效 | 未修复 |
| core/singing/ 7处 D:\AIvoice\ 硬编码 | 未修复 |
| 6平台调用 AI 但不发回复 (KOOK/Slack/钉钉等) | 未修复 |
| decision_hub content 变量被 reply 覆盖 | 未修复 |
| config/.env API Key 泄露 (需轮换) | 手动操作 |

---

## 三、关键文件索引

| 文件 | 用途 |
|------|------|
| .claude_context.md | 新会话交接文档 |
| BATCH_REVIEW.md | 46项修复清单 |
| FINAL_AUDIT.md | 6轮审计终版报告 |
| FIX_REPORT.md | 批量修复详情 |
| ROADMAP.md | 短/中/长期路线图 |
| STATUS.md | 当前状态总结 |

## 四、快速命令

```bash
# 启动守护进程
python -X utf8 run/daemon.py --api-port 9800

# 验证
timeout 30 python -X utf8 run/daemon.py --no-api 2>&1 | grep 已就绪

# 记忆统计
python -c "import sqlite3; print(sqlite3.connect('data/memory/miya_memory.db').execute('SELECT count(*) FROM memories').fetchone()[0])"

# 回退
git checkout backup-20260627-final
```
