# Miya 项目交接文档 · 新会话启动上下文

> 分支: `Ranxin` (52 commits) | 版本: v8.0.0 | Python 3.13 | DeepSeek V4 Flash  
> 最后更新: 2026-06-27

---

## 1. 项目概览与技术栈

**Miya (弥娅)** — AI 数字生命伴侣，22 人格，六层记忆，多平台 QQ 机器人。

| 层 | 技术 |
|------|------|
| 后端 | Python 3.13, FastAPI, asyncio, aiohttp |
| AI | DeepSeek V4 Flash (OpenAI 兼容协议), base_url=`https://api.deepseek.com/v1` |
| 记忆 | SQLite (主库, 2327条, WAL模式) + JSON文件 + Neo4j图库 (Docker, port 17687) |
| 消息 | M-Link 消息总线 + NapCatQQ OneBot 反向WS |
| 前端 | Electron + Vue 3 + Vite (miya_frontend/) + React Ops Center (frontend/ui/) |
| 配置 | 40+ JSON/YAML 在 config/, 核心: `text_config.json`(4000行), `multi_model_config.json` |
| MCP | 9 个 MCP 服务 (code_executor/database/filesystem/memory/naga_community/openclaw/pc_tracker/screen_vision/web_search) |

**入口**: `python run/daemon.py --api-port 9800`

---

## 2. 关键架构设计

### 数据流
```
NapCatQQ (反向WS) → OneBotPlatform (8095端口监听) → DecisionHub (3939行门面)
    → PerceptionHandler → SoulGenerator (情绪分析) → MemoryManager (记忆存储)
    → ResponseGenerator → ModelCollaborationEngine → _send_onebot_reply → QQ
```

### 记忆系统 (MiyaMemoryCore V3.1)
- **六层**: DIALOGUE → SHORT_TERM → LONG_TERM → SEMANTIC → KNOWLEDGE → PINNED
- **双后端**: JsonBackend (data/memory/ JSON) + SQLiteBackend (data/memory/miya_memory.db)
- **关键类**: `memory/core.py:782 MiyaMemoryCore`, `memory/core.py:355 JsonBackend`, `memory/sqlite_backend.py:89 SQLiteBackend`
- **检索加权**: 当前群×1.5, 当前用户×1.3, 标签匹配×1.2
- **锚点同步**: `_load_memory_anchors()` 用 mtime 比较, JSON 更新即同步到 SQLite

### 身份识别 (三层)
```python
is_superadmin(user_id) → is_owner=True → 系统提示词: "你的创造者{owner_name}, 保持亲密"
engine.is_staff(user_id)  → is_staff=True → 系统提示词: "然鑫的助理, 协作模式"
else                      → 普通用户     → 系统提示词: "AI助手, 保持距离"
```
- `{owner_name}` 运行时从 `permissions.json` superadmins 动态注入
- 权限文件: `config/permissions.json` (超级管理员: 869135903, 助理: 3141293329)

### 配置约定
- prompt_manager `{owner_name}` 占位, 不再硬编码名�"
- `config/.env` 含真实 API Key, `.gitignore` 排除
- 编码: `sys.stdout.reconfigure(encoding='utf-8')` 在 daemon.py
- 端口: daemon API=9800, OneBot反向WS=8095, Web API=8000(或8001), NapCat正向=3001(已废弃)

---

## 3. 已完成的工作

### 基础修复 (Commit 1-9 from original optimization)
- v7.0→v8.0 版本统一, BOM 清除, .gitignore 补漏
- 配置缓存: `memory/memory_config.py` 9→1次 I/O
- 写入防抖: working_memory 每条消息→5秒批量
- Neo4j 知识图谱全链路: GRAG五元组提取→Neo4j存储
- 安全: shell=True→shlex.split, exec 沙箱, CORS 修复, 凭据环境变量化

### 模块审计 & 修复 (455 项发现, 43+项修复)
- 并行投票 `split("")` → `split("</think>")` (collaboration_engine.py:984)
- OneBot 反向WS监听 (`_start_reverse_server`) — 核心改动
- `_do_health_check` 反向WS模式返回 True
- message_batcher 窗口满时先加消息再 flush (防丢失)
- session_manager 单例修复
- historian level 持久化 (update 参数新增 level)
- soul_generator 情绪分析不被噪声覆盖
- Windows 信号处理: `shutdown_event.set()` 替代 `asyncio.create_task`
- IntelligentExecutor `**kwargs` 兼容
- 三层身份识别: 创建者/助理/普通用户
- 记忆锚点 mtime 即时同步
- Desktop 终端回退到 Python (claude-code-engine 不可用)
- PC Time Tracker MCP 6工具 (mcpserver/pc_tracker/)

### 前端修复
- CoreApiClient 15缺失方法 + 端口8000→9800
- tts.ts isLoggedIn 修复
- Live2D 禁用 (缺失文件+噪音)
- TitleBar 版本显示

---

## 4. 当前未完结状态

### ⚠️ QQ回复可能未送达 (最新发现)
**症状**: AI 生成了 5 条响应, 但只有 3 条"私聊消息已发送"日志 -- 且这 3 条是 `proactive_chat` 主动聊天发的, 不是用户消息的回复。
**排查进度**: 已在 `_send_onebot_reply` 添加日志 (`b18532d7`). 旧日志没有"发送回复"记录。需要重启 daemon 后用 QQ 发消息, 看新日志是否出现 `发送回复` 来确定 `_send_onebot_reply` 是否被调用。
**相关**: `onebot_platform.py:809 _send_onebot_reply()`, `onebot_platform.py:731 route_to_decision_hub()`

### ⚠️ 其他已知问题
| 问题 | 影响 | 状态 |
|------|------|:---:|
| `jieba` 模块缺失 | 智能表情包发送失败 | 需 `pip install jieba` |
| `knowledge_graph` ERROR | Session 上下文管理 | 无影响 |
| Neo4j 本地连接失败 | Docker Desktop 未运行 | 知识图谱降级 |
| miya_api.py stub 路由 | ~30 API 返回空数据 | 未删除, Dashboard 不能用 |
| `core/singing/` 硬编码 D:\AIvoice\ | 唱歌功能非本机不可用 | 需环境变量化 |
| `memory/core.py` load() 无锁/backup 无锁/缓存无界 | 极端并发下可能出错 | 仅修复了语法错误 |

---

## 5. 遗留问题与痛点

### 安全
- `config/.env` 中的 DeepSeek + Tavily API Key 已暴露在本对话记录 (需轮换)
- `NEO4J_PASSWORD=MIYA123456` 弱密码
- `DEEPSEEK_API_KEY= sk-...` 等号后多余空格
- `config/qq_config.yaml` 中文注释全部乱码 (UTF-8→GBK 损坏)

### 架构债务
- `astrbot/` + `core/platform/` + `core/platform_astrbot/` 三重复制 (367文件)
- `decision_hub.py` 3939行 God Class
- 6平台 (KOOK/Slack/钉钉/Satori/微信/企微) 调AI但不发回复
- `mcpserver/openclaw` vendor 目录不存在
- `tests/` pytest 未安装, 0% 运行率

### 配置文件
- `text_config.json` 46% key 从未被使用
- 15/19 配置文件缺 `.example`
- `switch_model_config.bat` 脚本损坏

---

## 6. 立即下一步

### 第一步: 验证 QQ 回复送达 (紧急)
1. 重启 daemon: `python run/daemon.py --api-port 9800`
2. 用 QQ 发一条消息给 Miya
3. 查看日志: `grep "发送回复\|收到感知\|生成响应" logs/miya_*.log`
4. 如果 `收到感知` 后有 `发送回复` 日志 → 代码正常
5. 如果 `收到感知` 后有 `生成响应` 但没有 `发送回复` → `_send_onebot_reply` 未被调用, 需要追踪 `route_to_decision_hub` 的返回值和条件

### 第二步: 修复 miya_api.py stub 路由
- 删除 `core/web_api/miya_api.py` 第 2298-2600 行的重复 stub
- 这些 stub 覆盖了真正的 API 实现, 导致 Dashboard 全部返回空数据

### 第三步: 清理低可行性文件
- 删除 `astrbot/` (保留 `core/astrbot_compat/`)
- 删除 `core/platform_astrbot/`
- 删除 `core/platform_adapters/__init__.py`
- 被删文件的设计思路写入 `README.md` 未来规划

---

## 备份点
```
backup-20260610-2329     ← 修改前
backup-pre-batchfix      ← 批量修复前
backup-20260627-final    ← 当前最新
```

## 快速验证命令
```bash
# daemon 自检
timeout 30 python -X utf8 run/daemon.py --no-api --platforms webchat 2>&1 | grep "已就绪"

# import 测试
python -c "from memory.core import get_memory_core; from core.miya_daemon import MiyaDaemon; print('OK')"

# 记忆统计
python -c "import sqlite3; c=sqlite3.connect('data/memory/miya_memory.db').execute('SELECT count(*) FROM memories'); print(f'Memories: {c.fetchone()[0]}')"
```
